/*5. El área de un círculo se calcula de la siguiente manera: área = π x r x r. Escribe una función 
que calcule areaOfCircle*/

function areaOfCircle(r) {
  return console.log(`El area del circulo es igual a : ${3.1416 * r * r}`);
}

areaOfCircle(6);
