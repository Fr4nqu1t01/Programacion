/*2. El área de un rectángulo se calcula de la siguiente manera: área = largo x ancho. Escribe una 
función que calcule areaOfRectangle*/

function Area(Largo, Ancho) {
  return console.log(`El Area es igual a : ${Largo * Ancho}`);
}
Area(5, 8);
