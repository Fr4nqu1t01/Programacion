/*

1. **Números pares (1 al 100)**
   Usa un bucle `for` para imprimir en consola los números pares del 1 al 100.

2. **Suma de elementos de un arreglo**
   Declara un arreglo con 10 números. Usa un bucle para sumarlos y muestra el resultado con `console.log`.

3. **Contar positivos, negativos y ceros**
   Usa `prompt()` para pedir 10 números al usuario. Cuenta cuántos son positivos, negativos y ceros. Muestra los resultados.

4. **Buscar en arreglo**
   Declara un arreglo. Pide un número al usuario con `prompt()` y verifica si está en el arreglo. Usa `includes()` o un bucle.

5. **Tabla de multiplicar**
   Pide al usuario un número con `prompt()` y muestra su tabla de multiplicar del 1 al 10 usando un `for`.

6. **Números primos del 1 al 100**
   Usa un bucle para imprimir todos los números primos del 1 al 100 en la consola.

7. **Invertir arreglo**
   Declara un arreglo. Invierte su orden sin usar `.reverse()`. Muestra el resultado.

8. **Mayor y menor de un arreglo**
   Declara un arreglo. Usa un bucle para encontrar el mayor y menor número. Muestra ambos.

9. **Suma de dígitos**
   Pide un número con `prompt()`. Suma sus dígitos (ej: 123 → 1+2+3=6). Usa `split()` y `parseInt`.

10. **Contar vocales en una palabra**
    Pide una palabra con `prompt()` y cuenta cuántas vocales tiene. Considera solo vocales minúsculas.

¿Quieres que alguno de estos ejercicios tenga validación de entrada o manejo de errores?
*/