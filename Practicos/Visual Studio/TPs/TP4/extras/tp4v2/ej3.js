/*3. El perímetro de un rectángulo se calcula de la siguiente manera: perímetro = 2x (largo + 
ancho). Escribe una función que calcule el perímetro del rectángulo*/

function Perimetro(Largo, Ancho) {
  return console.log(`El perimetro es igual a: ${2 * (Largo + Ancho)}`);
}

Perimetro(3, 6);
