/*6. La circunferencia de un círculo se calcula de la siguiente manera: circunferencia = 2πr. 
Escribe una función que calcule circumOfCircle*/

function circumOfCircle(r) {
  return console.log(
    `La circunferencia del circulo es igual a: ${2 * 3.14 * r}`
  );
}
circumOfCircle(8);
