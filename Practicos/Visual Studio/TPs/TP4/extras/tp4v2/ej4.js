/*4. El volumen de un prisma rectangular se calcula de la siguiente manera: volumen = largo x 
ancho x alto. Escribe una función que calcule el volumenDeRectPrism*/

function Volumen(Largo, Ancho, Alto) {
  return console.log(
    `El volumen del prisma rectangular es igual a: ${Largo * Ancho * Alto}`
  );
}

Volumen(10, 3, 2);
